# Omnistation Plugin Plan Imports

This directory preserves the original OaC plugin integration plans that were generated in the Omnistation agent sandbox.

The files are kept as imported source artifacts so no planning work is lost while OaC execution moves to the local WSL workspace.

## Source

Original source directory:

```text
/home/ofunk/codex/agent-sandbox
```

Original source host:

```text
omnistation-omni-lsn-upq77
```

## Imported Files

See `SHA256SUMS` for byte-level verification hashes.

## Operating Decision

OaC execution is local in WSL under `~/OaC`.
Omnistation is not an OaC execution workspace.
