name: NaC Quality Gate

on:
  push:
    paths:
      - "processes/**/*.json"
      - "schemas/**/*.json"
      - "src/business_os/**/*.py"
      - "src/nac_bff/**/*.py"
      - "src/nac_cli/**/*.py"
      - "src/nac_m365_graph/**/*.py"
      - "tests/**/*.py"
      - "scripts/**/*.py"
      - "policies/**/*.yaml"
      - "policies/**/*.json"
      - "docs/**/*.md"
      - "assets/docs/**"
      - "**/*.adoc"
      - "**/*.asciidoc"
      - "prompts/**/*.md"
      - "deploy/**"
      - "plugins/**"
      - "workflows/**"
      - "usecases/**"
      - "roadmap/**"
      - "spfx/**"
      - ".codex/agents/**"
      - ".github/workflows/*.yml"
      - "AGENTS.md"
      - "README.md"
      - "pyproject.toml"
  pull_request:
    paths:
      - "processes/**/*.json"
      - "schemas/**/*.json"
      - "src/business_os/**/*.py"
      - "src/nac_bff/**/*.py"
      - "src/nac_cli/**/*.py"
      - "src/nac_m365_graph/**/*.py"
      - "tests/**/*.py"
      - "scripts/**/*.py"
      - "policies/**/*.yaml"
      - "policies/**/*.json"
      - "docs/**/*.md"
      - "assets/docs/**"
      - "**/*.adoc"
      - "**/*.asciidoc"
      - "prompts/**/*.md"
      - "deploy/**"
      - "plugins/**"
      - "workflows/**"
      - "usecases/**"
      - "roadmap/**"
      - "spfx/**"
      - ".codex/agents/**"
      - ".github/workflows/*.yml"
      - "AGENTS.md"
      - "README.md"
      - "pyproject.toml"
  workflow_dispatch:

jobs:
  spfx-bpmn-viewer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22.14.0"
          cache: npm
          cache-dependency-path: spfx/nac-bpmn-viewer/package-lock.json

      - name: Install pinned SPFx dependencies
        working-directory: spfx/nac-bpmn-viewer
        run: npm ci --ignore-scripts

      - name: Install pinned Playwright Chromium
        working-directory: spfx/nac-bpmn-viewer
        run: npx --no-install playwright install --with-deps chromium

      - name: Test, build and package BPMN viewer
        working-directory: spfx/nac-bpmn-viewer
        run: npm run build

      - name: Reproduce committed Workbench visual evidence
        working-directory: spfx/nac-bpmn-viewer
        run: |
          npm run workbench:capture
          npm run workbench:live:capture

      - name: Require deterministic Workbench visual evidence
        run: |
          git diff --exit-code -- assets/docs/generic-workbench
          git diff --exit-code -- assets/docs/workbench-live-read-binding

      - name: Upload generic workbench compiled evidence
        uses: actions/upload-artifact@v7
        with:
          name: generic-workbench-compiled
          if-no-files-found: error
          path: |
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/core/WorkbenchContracts.js
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/core/WorkbenchSelectors.js
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/core/parseWorkbenchSnapshot.js
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/nac/NacWorkbenchProjection.js
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/react/WorkbenchPanel.js
            spfx/nac-bpmn-viewer/lib-commonjs/workbench/react/WorkbenchPanel.styles.js

      - name: Upload live workbench host compiled evidence
        uses: actions/upload-artifact@v7
        with:
          name: workbench-live-host-compiled
          if-no-files-found: error
          path: |
            spfx/nac-bpmn-viewer/lib-commonjs/webparts/nacBpmnViewer/components/NacWorkbenchHost.js
            spfx/nac-bpmn-viewer/lib-commonjs/webparts/nacBpmnViewer/components/NacWorkbenchHost.styles.js

  quality-gate:
    needs: spfx-bpmn-viewer
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Require successful SPFx evidence build
        env:
          SPFX_EVIDENCE_RESULT: ${{ needs.spfx-bpmn-viewer.result }}
        run: |
          if [ "$SPFX_EVIDENCE_RESULT" != "success" ]; then
            echo "SPFx evidence build did not complete successfully: $SPFX_EVIDENCE_RESULT" >&2
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install package
        run: pip install -e . fastapi==0.116.1 httpx==0.28.1

      - name: Download generic workbench compiled evidence
        uses: actions/download-artifact@v7
        with:
          name: generic-workbench-compiled
          path: spfx/nac-bpmn-viewer/lib-commonjs/workbench

      - name: Download live workbench host compiled evidence
        uses: actions/download-artifact@v7
        with:
          name: workbench-live-host-compiled
          path: spfx/nac-bpmn-viewer/lib-commonjs/webparts/nacBpmnViewer/components

      - name: Install pinned Bicep compiler
        run: |
          az bicep uninstall
          az bicep install --version v0.45.6

      - name: Reproduce Azure BFF Bicep artifacts
        run: |
          az bicep build --file deploy/runtime/azure/nac-bff/infra/main.bicep --stdout > /tmp/nac-bff-main.json
          az bicep build-params --file deploy/runtime/azure/nac-bff/infra/main.example.bicepparam --stdout > /tmp/nac-bff-main-params.json
          cmp /tmp/nac-bff-main.json deploy/runtime/azure/nac-bff/infra/compiled/main.json
          cmp /tmp/nac-bff-main-params.json deploy/runtime/azure/nac-bff/infra/compiled/main.example.json

      - name: Compile Azure WORM Bicep baseline
        run: az bicep build --file deploy/runtime/azure/immutable-evidence/main.bicep --stdout > /tmp/nac-worm-main.json

      - name: Install pinned performance coordination Bicep compiler
        run: |
          az bicep uninstall
          az bicep install --version v0.45.15

      - name: Reproduce and validate Azure BFF performance coordination Bicep
        env:
          DOTNET_BUNDLE_EXTRACT_BASE_DIR: /tmp/nac-bff-dotnet
        run: |
          "$HOME/.azure/bin/bicep" build deploy/runtime/azure/nac-bff-performance-coordination/main.bicep --stdout > /tmp/nac-bff-performance-coordination-main.json
          "$HOME/.azure/bin/bicep" build-params deploy/runtime/azure/nac-bff-performance-coordination/main.example.bicepparam --stdout > /tmp/nac-bff-performance-coordination-main-params.json
          cmp /tmp/nac-bff-performance-coordination-main.json deploy/runtime/azure/nac-bff-performance-coordination/compiled/main.json
          cmp /tmp/nac-bff-performance-coordination-main-params.json deploy/runtime/azure/nac-bff-performance-coordination/compiled/main.example.json
          python scripts/validate_nac_bff_performance_coordination_arm.py /tmp/nac-bff-performance-coordination-main.json /tmp/nac-bff-performance-coordination-main-params.json

      - name: Run strict quality gate
        env:
          NAC_REQUIRE_WORKBENCH_BUILD_EVIDENCE: "1"
        run: python scripts/quality_gate.py --profile strict

      - name: Render PR developer CI comment
        if: always() && github.event_name == 'pull_request'
        run: python scripts/render_quality_gate_comment.py --input out/quality/status.json --output out/quality/comment.md

      - name: Upsert PR developer CI comment
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- nac-quality-gate-comment -->';
            const body = fs.readFileSync('out/quality/comment.md', 'utf8');
            const issue_number = context.payload.pull_request.number;
            const comments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number,
                per_page: 100,
              }
            );
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
              core.info(`Updated quality gate comment ${existing.id}`);
            } else {
              const created = await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number,
                body,
              });
              core.info(`Created quality gate comment ${created.data.id}`);
            }

      - name: Upload quality gate artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: nac-quality-gate
          path: out/quality/
