name: Export Documentation PDF

on:
  workflow_dispatch:

jobs:
  export-pdf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-latex-base texlive-latex-extra texlive-fonts-recommended texlive-xetex

      - name: Build German NaC handbook PDF
        run: |
          mkdir -p out/de
          pandoc \
            README.md \
            docs/de/START_HERE.md \
            docs/de/fachanwender-guide.md \
            docs/de/notariat-as-code.md \
            docs/de/governance.md \
            docs/de/technology-decision.md \
            docs/de/public-readiness.md \
            --from gfm \
            --toc \
            --number-sections \
            --pdf-engine=xelatex \
            -o out/de/nac-notariat-handbook.pdf

      - name: Build English NaC handbook PDF
        run: |
          mkdir -p out/en
          pandoc \
            README.md \
            docs/en/START_HERE.md \
            docs/en/fachanwender-guide.md \
            docs/en/notariat-as-code.md \
            docs/en/governance.md \
            docs/en/technology-decision.md \
            docs/en/public-readiness.md \
            --from gfm \
            --toc \
            --number-sections \
            --pdf-engine=xelatex \
            -o out/en/nac-notariat-handbook.pdf

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: nac-notariat-handbook-pdf
          path: out/
